# Task 2002 done_when

- `cargo check` がエラーなく完了する
- `src/ui/widgets/permission_badge.rs` に `permission_span`, `permission_line`, `permission_summary_text` の 3 関数が存在する
- `permission_span("shell", true, &theme)` が danger 色の Span を返す
- `permission_span("shell", false, &theme)` が success 色の Span を返す
- `permission_span("network", true, &theme)` が danger 色の Span を返す
- `permission_span("filesystem_write", true, &theme)` が warning 色の Span を返す
- `permission_span("filesystem_read", true, &theme)` が text_secondary 色の Span を返す
- `permission_line` が name と状態を含む Line を返す
- `permission_summary_text` が danger 権限を赤系、safe 権限を緑系で返す
