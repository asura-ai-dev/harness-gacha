# Task 5005: サンプル pack データ充実と統合確認

## 概要

サンプル pack を 5 件以上に増やし、全画面遷移と主要ユーザーフローを通しで確認できる状態にする。カタログ画面での [OWNED] ラベル表示も接続する。

## 詳細

### 1. `data/catalog.json` のサンプル pack 追加

既存の 3 pack に加えて、少なくとも 2 pack を追加し、合計 5 pack 以上にする。追加 pack の条件:

**Pack 4: Release Note Helper**

- id: "release-note-helper"
- price: 900, featured: false, status: "listed"
- permissions: shell=false, network=false, filesystem_read=true, filesystem_write=false, git=false
- targets: claude-code >=1.0.0
- tags: ["release", "documentation"]
- contents_summary: skills=1, hooks=1, templates=3, other=0
- sample_preview: 日本語の短い文章

**Pack 5: Handoff Pack**

- id: "handoff-pack"
- price: 1300, featured: true, status: "listed"
- permissions: shell=false, network=false, filesystem_read=true, filesystem_write=true, git=false
- targets: claude-code >=1.0.0, codex >=0.9.0
- tags: ["handoff", "team", "workflow"]
- contents_summary: skills=2, hooks=1, templates=3, other=1
- sample_preview: 日本語の短い文章

各 pack の author は異なる名前にする（バリエーション確認のため）。checkout_url はダミーのまま。risks と review_notes は一部の pack に設定する。

### 2. `data/entitlements.json` の更新

追加した pack のうち 1 件を entitlement に追加する。合計 3 件の entitlement にする。installed=false の pack を 1 件以上含める。

### 3. `data/accounting.json` の更新

追加 pack に対応する transaction を追加する。合計 5 件以上の transaction にする。refund の transaction も 1 件含める。

### 4. カタログ画面の [OWNED] ラベル表示

`src/ui/catalog.rs` の `render_pack_list` で、entitlement を持つ pack に `[OWNED]` ラベルを付与する。

```rust
// render_pack_list 内の items 構築で追加
let is_owned = match &app.entitlements {
    Some(store) => crate::data::entitlement::is_owned(store, &pack.id),
    None => false,
};
let owned_label = if is_owned {
    Span::styled(" [OWNED]", Style::default().fg(theme.owned))
} else {
    Span::raw("")
};
// ListItem の行に owned_label を追加
```

### 5. 全画面遷移の接続確認

以下の遷移パスが全て動作することを確認し、必要に応じて修正する:

1. カタログ -> Pack 詳細 -> 安全性詳細 -> 購入案内 -> (Back) -> 安全性詳細 -> (Back) -> Pack 詳細 -> (Back) -> カタログ
2. カタログ -> Discovery（待機 -> 演出 -> 結果） -> Pack 詳細 -> 購入案内
3. カタログ -> 購入済みライブラリ -> インストール詳細 -> (Back) -> ライブラリ -> (Back) -> カタログ
4. カタログ -> ヘルプ -> (Back) -> カタログ
5. Discovery 結果 -> もう一度探す -> 演出 -> 結果

`src/ui/render.rs` で全 8 画面（Catalog, Discovery, PackDetail, SafetyDetail, Purchase, Library, InstallDetail, Help）がプレースホルダーではなく実際の描画関数に接続されていることを確認する。

### 6. render.rs の最終接続

全画面の import と render 呼び出しが揃っていることを確認する。

```rust
use super::{catalog, discovery, pack_detail, safety_detail, purchase, library, install_detail, help};

pub fn render(frame: &mut Frame, app: &App) {
    let theme = cherry_cartridge();
    // 背景
    let bg = Block::default().style(Style::default().bg(theme.primary_bg));
    frame.render_widget(bg, frame.area());

    match app.current_screen {
        Screen::Catalog => catalog::render(frame, app, &theme),
        Screen::Discovery => discovery::render(frame, app, &theme),
        Screen::PackDetail => pack_detail::render(frame, app, &theme),
        Screen::SafetyDetail => safety_detail::render(frame, app, &theme),
        Screen::Purchase => purchase::render(frame, app, &theme),
        Screen::Library => library::render(frame, app, &theme),
        Screen::InstallDetail => install_detail::render(frame, app, &theme),
        Screen::Help => help::render(frame, app, &theme),
    }
}
```

## depends_on

task-5001, task-5002, task-5003, task-5004, task-4004, task-3003

## type

data
