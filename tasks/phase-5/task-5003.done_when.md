# Task 5003 done_when

- `cargo build` が成功する
- カタログ画面で `/` キーを押すと検索モードに入る（タイトルバーに検索クエリが表示される）
- 検索モード中に文字を入力するとリアルタイムでリストがフィルタされる
- 検索クエリが pack の name または summary に部分一致する pack のみが表示される
- 検索クエリを全て削除すると全 listed pack が再表示される
- `Esc` で検索モードを終了できる
- カタログ画面で `t` キーを押すとタグ絞り込みがサイクルする（None -> tag1 -> tag2 -> ... -> None）
- アクティブタグがある場合、該当タグを持つ pack のみが表示される
- タブ行にアクティブタグ名が表示される（Tag: {tag} または Tag: all）
- 検索とタグ絞り込みの併用が可能（両条件の AND フィルタ）
- フィルタ結果が空の場合にリストが空になりクラッシュしない
- `src/data/catalog.rs` に `search_packs`, `filter_by_tag`, `all_tags` の 3 関数が存在する
- `cargo test` で検索とタグ絞り込みのテストが pass する
