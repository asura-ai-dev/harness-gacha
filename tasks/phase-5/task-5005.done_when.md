# Task 5005 done_when

- `cargo build` が成功し、`cargo run` で TUI が起動する
- `data/catalog.json` に 5 件以上の pack データが存在する
- `data/entitlements.json` に 3 件以上の entitlement が存在する
- `data/accounting.json` に 5 件以上の transaction と refund transaction が 1 件以上存在する
- カタログ画面で購入済み pack に `[OWNED]` ラベルが owned 色（青系）で表示される
- 未購入 pack に `[OWNED]` ラベルが表示されない
- 以下の遷移パスが全て動作する:
  - カタログ -> Pack 詳細 -> 安全性詳細 -> 購入案内 -> Back -> 安全性詳細 -> Back -> Pack 詳細 -> Back -> カタログ
  - カタログ -> Discovery 待機 -> Enter -> 演出（約 1.5 秒） -> 結果 -> Enter -> Pack 詳細
  - Discovery 結果 -> r でもう一度探す -> 演出 -> 結果
  - カタログ -> L で購入済みライブラリ -> Enter でインストール詳細 -> Back -> ライブラリ -> Back -> カタログ
  - カタログ -> ? でヘルプ -> Back -> カタログ
- `src/ui/render.rs` で全 8 画面（Catalog, Discovery, PackDetail, SafetyDetail, Purchase, Library, InstallDetail, Help）が実際の描画関数に接続されている（プレースホルダーが残っていない）
- Cherry Cartridge カラーパレットが全画面で適用されている（背景色 #151018、アクセント色 #FF7A59 等）
- `cargo test` で全テストが pass する
