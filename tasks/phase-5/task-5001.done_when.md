# Task 5001 done_when

- `data/accounting.json` が存在し、3 件以上の transaction と 2 件以上の creator_share が記述されている
- `data/accounting.json` を `serde_json::from_str::<AccountingData>` でパースしてエラーが出ない
- `src/data/accounting.rs` に `load_accounting`, `save_accounting`, `gross_sales_for_pack`, `refunds_for_pack`, `net_sales_for_pack`, `transactions_for_month`, `calculate_monthly_payouts` の 7 関数が存在する
- `cargo test` で `test_gross_sales`, `test_refunds`, `test_net_sales`, `test_monthly_payouts`, `test_monthly_payouts_empty_month` の 5 テストが全て pass する
- `calculate_monthly_payouts` が purchase と refund を正しく差し引いて net sales を算出する
- `calculate_monthly_payouts` が creator_shares の share_rate を乗じた分配額を返す
- 対象月に transaction がない場合に分配額が 0 になる
- `load_accounting` に存在しないパスを渡した場合にパニックせず空の AccountingData を返す
- `App::new()` で accounting.json が読み込まれ、App の accounting フィールドにデータが設定される
