# Task 5004 done_when

- `cargo build` が成功する
- `src/error.rs` に `AppError` enum が定義され、DataLoad, JsonParse, Io, BrowserOpen, Clipboard のバリアントを持つ
- `main()` が `Result` を返し、エラー時もターミナルが復元される
- panic 時にターミナルが raw mode から復元される（`install_panic_hook` が main の先頭で呼ばれている）
- `data/catalog.json` が存在しない状態で `cargo run` を実行してもパニックしない（空のカタログが表示される）
- カタログが空の場合に "カタログデータが見つかりません" または "pack が見つかりません" が表示される
- フィルタ結果が 0 件の場合にカタログ画面がクラッシュしない
- `data/entitlements.json` が存在しない状態で `cargo run` を実行してもパニックしない
- `data/accounting.json` が存在しない状態で `cargo run` を実行してもパニックしない
- `run_app` 関数がイベントループを実行し、正常終了時に Ok(()) を返す
