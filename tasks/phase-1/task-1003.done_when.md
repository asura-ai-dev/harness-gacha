# Task 1003 done_when

- `data/catalog.json` が存在し、3 件以上の pack データが JSON 配列として記述されている
- `data/catalog.json` を `serde_json::from_str::<Vec<CatalogEntry>>` でパースしてエラーが出ない（`cargo test` で検証可能なテストを含む）
- `src/data/catalog.rs` に `load_catalog`, `listed_packs`, `featured_packs`, `recent_packs`, `find_pack_by_id` の 5 関数が存在する
- `load_catalog` に存在しないパスを渡した場合にパニックせず空の Vec を返す
- `find_pack_by_id` で存在する id を指定すると Some が返る
- `find_pack_by_id` で存在しない id を指定すると None が返る
- `featured_packs` が featured=true かつ status="listed" の pack のみを返す
- `cargo check` がエラーなく完了する
