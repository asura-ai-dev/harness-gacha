# Task 1005: Cherry Cartridge テーマと Screen/Action enum 定義

## 概要

Cherry Cartridge カラーパレットの Theme 構造体、Screen enum（画面遷移状態）、Action enum（ユーザー操作の抽象化）を定義する。

## 詳細

### 1. `src/ui/theme.rs`

`agent-docs/ui-components.md` のテーマ定義セクションに従い実装する。

```rust
use ratatui::style::{Color, Style, Modifier};

pub struct Theme {
    pub primary_bg: Color,
    pub panel_bg: Color,
    pub border: Color,
    pub text_primary: Color,
    pub text_secondary: Color,
    pub accent: Color,
    pub accent_alt: Color,
    pub success: Color,
    pub warning: Color,
    pub danger: Color,
    pub owned: Color,
}

pub fn cherry_cartridge() -> Theme {
    Theme {
        primary_bg: Color::Rgb(0x15, 0x10, 0x18),
        panel_bg: Color::Rgb(0x24, 0x1A, 0x28),
        border: Color::Rgb(0x5B, 0x3A, 0x4A),
        text_primary: Color::Rgb(0xF7, 0xE8, 0xD8),
        text_secondary: Color::Rgb(0xD9, 0xB8, 0xA7),
        accent: Color::Rgb(0xFF, 0x7A, 0x59),
        accent_alt: Color::Rgb(0xFF, 0xD1, 0x66),
        success: Color::Rgb(0x7B, 0xD3, 0x89),
        warning: Color::Rgb(0xF4, 0xB9, 0x42),
        danger: Color::Rgb(0xE1, 0x56, 0x56),
        owned: Color::Rgb(0x6E, 0xC5, 0xE9),
    }
}

impl Theme {
    /// 主テキスト用スタイル
    pub fn text_style(&self) -> Style {
        Style::default().fg(self.text_primary).bg(self.primary_bg)
    }

    /// 補助テキスト用スタイル
    pub fn secondary_style(&self) -> Style {
        Style::default().fg(self.text_secondary).bg(self.primary_bg)
    }

    /// パネル背景用スタイル
    pub fn panel_style(&self) -> Style {
        Style::default().fg(self.text_primary).bg(self.panel_bg)
    }

    /// ボーダー用スタイル
    pub fn border_style(&self) -> Style {
        Style::default().fg(self.border)
    }

    /// 選択行のハイライトスタイル
    pub fn highlight_style(&self) -> Style {
        Style::default().fg(self.primary_bg).bg(self.accent).add_modifier(Modifier::BOLD)
    }

    /// フッターのキーバインド表示用スタイル
    pub fn footer_key_style(&self) -> Style {
        Style::default().fg(self.accent).add_modifier(Modifier::BOLD)
    }

    /// フッターのテキスト用スタイル
    pub fn footer_text_style(&self) -> Style {
        Style::default().fg(self.text_secondary)
    }
}
```

### 2. `src/screen.rs`

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Screen {
    Catalog,
    Discovery,
    PackDetail,
    SafetyDetail,
    Purchase,
    Library,
    InstallDetail,
    Help,
}
```

### 3. `src/action.rs`

```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Action {
    Quit,
    Back,
    Enter,
    Up,
    Down,
    Left,
    Right,
    Tab,
    BackTab,
    Search,
    SearchInput(char),
    SearchBackspace,
    ToggleTag,
    ToggleDiscovery,
    OpenLibrary,
    OpenHelp,
    OpenSafety,
    OpenPurchase,
    OpenCheckout,
    CopyUrl,
    Tick,
    None,
}
```

## depends_on

task-1001

## type

code
