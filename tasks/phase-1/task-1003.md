# Task 1003: サンプルデータ作成と JSON 読み込み層

## 概要

`data/catalog.json` にサンプルカタログデータ（3 pack 以上）を作成し、`src/data/catalog.rs` に JSON 読み込み関数を実装する。

## 詳細

### 1. `data/catalog.json` の作成

プロジェクトルート直下の `data/catalog.json` に 3 件以上の pack データを JSON 配列で記述する。`agent-docs/data-models.md` の JSON ファイル構造セクションの形式に従う。

以下の 3 pack を含めること（内容は適宜調整してよいが、フィールドは全て含めること）:

**Pack 1: Team Review Pack**

- id: "team-review-pack"
- price: 1800, featured: true, status: "listed"
- permissions: shell=true, network=false, filesystem_read=true, filesystem_write=true, git=false
- targets: claude-code >=1.0.0, codex >=0.9.0
- tags: ["review", "team", "typescript"]
- contents_summary: skills=3, hooks=2, templates=4, other=0

**Pack 2: Frontend Spec Kit**

- id: "frontend-spec-kit"
- price: 1200, featured: true, status: "listed"
- permissions: shell=false, network=false, filesystem_read=true, filesystem_write=false, git=false
- targets: claude-code >=1.0.0
- tags: ["frontend", "spec", "react"]
- contents_summary: skills=2, hooks=0, templates=5, other=1

**Pack 3: Bug Triage Starter**

- id: "bug-triage-starter"
- price: 1500, featured: false, status: "listed"
- permissions: shell=true, network=true, filesystem_read=true, filesystem_write=true, git=true
- targets: codex >=0.9.0
- tags: ["bug", "triage", "workflow"]
- contents_summary: skills=1, hooks=3, templates=2, other=0

各 pack の checkout*url は `"https://buy.stripe.com/example*{id}"` 形式のダミー URL でよい。
sample_preview は日本語の短い文章（1-2 行）を入れる。
listed_at と updated_at は ISO 8601 形式の日時文字列。

### 2. `src/data/catalog.rs` の実装

```rust
use std::path::Path;
use crate::models::CatalogEntry;

/// catalog.json を読み込み、CatalogEntry のベクタを返す
/// ファイルが存在しない場合やパースエラー時は空のベクタを返す
pub fn load_catalog(path: &Path) -> Vec<CatalogEntry> {
    // 1. std::fs::read_to_string でファイルを読む
    // 2. serde_json::from_str::<Vec<CatalogEntry>> でパース
    // 3. エラー時は eprintln でメッセージを出力し、空 Vec を返す
}

/// status が "listed" の pack のみをフィルタして返す
pub fn listed_packs(catalog: &[CatalogEntry]) -> Vec<&CatalogEntry> {
    catalog.iter().filter(|p| p.status == "listed").collect()
}

/// featured == true の pack をフィルタして返す
pub fn featured_packs(catalog: &[CatalogEntry]) -> Vec<&CatalogEntry> {
    catalog.iter().filter(|p| p.featured && p.status == "listed").collect()
}

/// listed_at の降順でソートした pack を返す（recent）
pub fn recent_packs(catalog: &[CatalogEntry]) -> Vec<&CatalogEntry> {
    let mut packs: Vec<&CatalogEntry> = listed_packs(catalog);
    packs.sort_by(|a, b| b.listed_at.cmp(&a.listed_at));
    packs
}

/// id で pack を検索して返す
pub fn find_pack_by_id<'a>(catalog: &'a [CatalogEntry], id: &str) -> Option<&'a CatalogEntry> {
    catalog.iter().find(|p| p.id == id)
}
```

### 3. `src/data/mod.rs` の更新

`pub mod catalog;` が宣言されていることを確認する（task-1001 で作成済みのはず）。

## depends_on

task-1001, task-1002

## type

data
