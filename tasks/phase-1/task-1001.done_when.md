# Task 1001 done_when

- `cargo check` がエラーなく完了する
- `cargo build` が成功する
- `Cargo.toml` に ratatui, crossterm, serde, serde_json, chrono, rand, open, qrcode, arboard, thiserror が依存として記載されている
- `src/main.rs` が存在し、`cargo run` で実行できる（出力内容は問わない）
- `src/models/mod.rs` が存在し、`catalog_entry`, `entitlement`, `accounting`, `manifest`, `permissions` の 5 モジュールを pub mod で公開している
- `src/data/mod.rs` が存在し、`catalog`, `entitlement`, `accounting`, `manifest` の 4 モジュールを pub mod で公開している
- `src/ui/mod.rs` が存在し、`render`, `catalog`, `discovery`, `pack_detail`, `safety_detail`, `purchase`, `library`, `install_detail`, `help`, `theme`, `widgets` の 11 モジュールを pub mod で公開している
- `src/ui/widgets/mod.rs` が存在し、`capsule_machine`, `pack_card`, `permission_badge`, `qr_code` の 4 モジュールを pub mod で公開している
- `data/` ディレクトリ（プロジェクトルート直下、src/ の外）が存在する
