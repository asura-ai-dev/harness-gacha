# Task 1005 done_when

- `cargo check` がエラーなく完了する
- `src/ui/theme.rs` に `Theme` struct と `cherry_cartridge()` 関数が存在する
- `Theme` の全 11 フィールド（primary_bg, panel_bg, border, text_primary, text_secondary, accent, accent_alt, success, warning, danger, owned）が `Color::Rgb` で定義されている
- `Theme` に `text_style()`, `secondary_style()`, `panel_style()`, `border_style()`, `highlight_style()`, `footer_key_style()`, `footer_text_style()` の 7 メソッドが存在する
- `src/screen.rs` に `Screen` enum が 8 バリアント（Catalog, Discovery, PackDetail, SafetyDetail, Purchase, Library, InstallDetail, Help）で定義されている
- `Screen` に `Clone`, `Copy`, `PartialEq`, `Eq` derive が付与されている
- `src/action.rs` に `Action` enum が定義され、Quit, Back, Enter, Up, Down, Left, Right, Tab, Tick, None を含む
