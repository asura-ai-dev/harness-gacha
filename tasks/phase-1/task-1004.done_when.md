# Task 1004 done_when

- `cargo test` で以下の 5 テストが全て pass する: `test_valid_manifest`, `test_empty_id`, `test_invalid_id_chars`, `test_empty_targets`, `test_invalid_install_method`
- `src/data/manifest.rs` に `load_manifest`, `validate_manifest`, `validate_contents` の 3 関数が存在する
- `validate_manifest` が id の空文字・不正文字、name の空文字、targets の空配列、install.method の不正値をそれぞれ検出してエラーに含める
- `ValidationResult` struct が `valid: bool` と `errors: Vec<String>` を持つ
- 必須フィールド（id, name, version, summary, author.name, targets, contents, install.method, license.type）の欠落が検出される
- `cargo check` がエラーなく完了する
