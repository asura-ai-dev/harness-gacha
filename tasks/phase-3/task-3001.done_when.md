# Task 3001 done_when

- `cargo check` がエラーなく完了する
- `src/discovery.rs` に `pick_random_pack` 関数が存在し、listed な pack からランダムに 1 つの id を返す
- 空のカタログに対して `pick_random_pack` が None を返す（`cargo test` で検証）
- `src/ui/widgets/capsule_machine.rs` に `capsule_art`, `capsule_lines`, `idle_art` 関数と `TOTAL_FRAMES` 定数が存在する
- `capsule_art(0)` から `capsule_art(5)` まで 6 フレーム分の異なる ASCII アート文字列配列を返す
- `TOTAL_FRAMES` が 6 である
- `capsule_lines` がフレーム番号と theme を受け取り `Vec<Line>` を返す
- `src/ui/widgets/pack_card.rs` に `pack_card_lines` 関数が存在し、CatalogEntry から Line ベクタを返す
- pack_card_lines の出力に pack 名、summary、creator、targets、price、trust、contents_summary が含まれる
