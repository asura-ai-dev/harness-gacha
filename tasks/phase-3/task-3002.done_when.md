# Task 3002 done_when

- `cargo build` が成功する
- カタログ画面で `r` キーを押すと Discovery 画面（待機状態）に遷移する
- 待機状態でガチャガチャマシンの ASCII アートが中央に表示される
- "PRESS ENTER TO DISCOVER" が accent 色で表示される
- "無料でおすすめ pack を 1 つ表示します" が表示される
- フッターに "[Enter] ランダムで探す [b] 戻る" が表示される
- 待機状態で `Enter` を押すと演出状態に遷移する（task-3003 で接続するため、この時点では遷移しなくてもよいが、描画関数自体は 3 状態すべて実装されていること）
- `render_idle`, `render_animating`, `render_result` の 3 関数が `src/ui/discovery.rs` に存在する
- render_animating がフレーム番号に応じて異なるアートとステータステキスト（"turning..." 等）を表示する
- render_result が "[ Capsule Opened ]" と pack カード情報を表示する
- `b` または `Esc` でカタログ画面に戻る
- `Screen::Discovery` が render.rs のディスパッチに接続されている
