# Task 4001 done_when

- `cargo build` が成功する
- Pack 詳細画面で `p` キーを押すと購入案内画面に遷移する
- 安全性詳細画面で `p` キーを押すと購入案内画面に遷移する
- 購入案内画面にヘッダー "Checkout: {pack名}" が表示される
- Price セクションに "YEN {price}" が表示される
- Seller セクションに "harness-gacha" が表示される
- Policy セクションに返金ポリシー要約テキストが表示される
- Checkout URL セクションに pack の checkout_url が表示される
- QR Code セクションにテキストベースの QR コードが描画される
- `Enter` キーでデフォルトブラウザが起動する（URL が開かれる）
- ブラウザ起動後にフッターに "ブラウザを起動しました" メッセージが表示される
- `c` キーで checkout_url がシステムクリップボードにコピーされる
- コピー後にフッターに "URL をコピーしました" メッセージが表示される
- メッセージが一定時間後に自動でクリアされる
- `b` または `Esc` で前の画面に戻る
- `src/browser.rs` に `open_url` 関数が存在する
- `src/clipboard.rs` に `copy_to_clipboard` 関数が存在する
- `src/ui/widgets/qr_code.rs` に `generate_qr_lines` 関数が存在する
