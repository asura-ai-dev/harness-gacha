# Task 4002 done_when

- `data/entitlements.json` が存在し、2 件以上の entitlement データが記述されている
- `data/entitlements.json` を `serde_json::from_str::<EntitlementStore>` でパースしてエラーが出ない
- `src/data/entitlement.rs` に `load_entitlements`, `save_entitlements`, `active_entitlements`, `is_owned`, `find_entitlement` の 5 関数が存在する
- `cargo test` で `test_active_entitlements`, `test_is_owned`, `test_find_entitlement` の 3 テストが pass する
- `load_entitlements` に存在しないパスを渡した場合にパニックせず空の EntitlementStore を返す
- `is_owned` が status="active" の pack に対して true を返す
- `is_owned` が status="refunded" の pack に対して false を返す
- `App::new()` で entitlements.json が読み込まれ、App の entitlements フィールドにデータが設定される
- `cargo check` がエラーなく完了する
