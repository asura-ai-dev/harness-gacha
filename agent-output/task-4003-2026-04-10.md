# Task 4003 Handoff

## Summary

- Implemented purchased library screen in `src/ui/library.rs`.
- Wired `Screen::Library` rendering in `src/ui/render.rs`.
- Added `Screen::Library` action handling and active entitlement count helper in `src/app.rs`.

## Implementation Notes

- Library list is driven by `entitlement::active_entitlements(&app.entitlements)`.
- Each row resolves catalog metadata via `find_pack_by_id`, then shows pack name, catalog version, install status, and update availability.
- `Enter` on the selected library item stores `selected_pack_id` and navigates to `Screen::InstallDetail`.
- `Up` and `Down` update `library_state.selected_index` within the active entitlement bounds.
- The code was adapted to the current codebase shape where `app.entitlements` is `EntitlementStore`, not `Option<EntitlementStore>`.

## Verification

- Ran `cargo build` in `/Users/kzt/Desktop/project-d/product/harness-gacha`.
- Result: success, compile errors zero.
- Existing repository warnings remain, but no new build failures were introduced by this task.

## Changed Files

- `src/ui/library.rs`
- `src/ui/render.rs`
- `src/app.rs`
