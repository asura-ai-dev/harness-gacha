# Task 2003 Handoff

## Summary

- Implemented the safety detail screen in `src/ui/safety_detail.rs`.
- Added `Screen::SafetyDetail` dispatch in `src/ui/render.rs`.
- Added `Screen::SafetyDetail` action handling in `src/app.rs`.

## Details

- The safety detail screen renders a 3-part vertical layout:
  - header: `Safety Details: {pack_name}`
  - content: permissions, risks, review notes
  - footer: `[p] 購入案内  [b] 詳細へ戻る`
- Permissions are rendered with `permission_badge::permission_line()`.
- Danger permissions (`shell`, `network`) and warning permissions (`filesystem_write`, `git`) inherit existing badge styling from `permission_badge.rs`.
- Risks are rendered as warning-colored `- {risk}` lines when present.
- Review notes are rendered in `theme.text_secondary` when present.
- Missing pack state renders `Pack not found`.

## Verification

- Ran `cargo build`
- Result: success

## Acceptance Check

- `tasks/phase-2/task-2003.md` requirements implemented
- `cargo build` succeeded
- `agent-output/task-2003-2026-04-10.md` created
