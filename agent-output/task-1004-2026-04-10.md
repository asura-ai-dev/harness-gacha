# task-1004: manifest.json バリデーター

- 目的: `manifest.json` の読み込みと必須項目バリデーションを `src/data/manifest.rs` に実装
- 開始日: 2026-04-10
- 更新日: 2026-04-10

## Current Phase

- Phase 1
- Status: Complete (pass)

## Completed

- `ValidationResult { valid, errors }` を実装
- `load_manifest(path: &Path) -> Result<Manifest, String>` を実装
- `validate_manifest(manifest: &Manifest) -> ValidationResult` を実装
- `validate_contents(manifest: &Manifest, pack_root: &Path) -> Vec<String>` を実装
- `valid_manifest` ヘルパーと指定 5 テストを追加
- `src/data/mod.rs` に `pub mod manifest;` が既に存在することを確認
- `cargo check` 成功を確認
- `cargo test` で 5 テスト pass を確認

## Incomplete

- なし

## Failed Tests

- なし

## Important Decisions

- `id` は空文字チェックを先に行い、空でない場合のみ文字種チェックを実行
- `install.method` は `matches!` で `copy | script | manual` のみ許可
- `validate_contents` は pack ルートからの相対パスを `join` し、`exists()` で存在確認
- エラーメッセージは task-1004 の仕様文言に寄せて実装

## Next Step

- 実際の pack サンプルに対して `load_manifest` と `validate_contents` を呼ぶ統合側の利用箇所を追加する
