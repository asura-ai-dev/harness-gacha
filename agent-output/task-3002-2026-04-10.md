# Task 3002 Handoff

## Summary

- Implemented the Discovery screen in `src/ui/discovery.rs` with the 3 requested states: idle, animating, and result.
- Added `Screen::Discovery` rendering in `src/ui/render.rs`.
- Added Discovery action handling in `src/app.rs` for animation start, animation frame progression on `Tick`, result confirmation on `Enter`, and rerun on `r`.

## Details

- Idle state renders the capsule machine art, CTA text, and footer prompt per `tasks/phase-3/task-3002.md`.
- Animating state renders the capsule machine frames with frame-dependent status text and a non-interactive footer.
- Result state renders `[ Capsule Opened ]` plus `pack_card::pack_card_lines(...)`, and falls back to `Pack not found` if the catalog lookup fails.
- Discovery animation uses the existing `pick_random_pack(...)` logic and advances from `Animating` to `Result` after `capsule_machine::TOTAL_FRAMES`.
- Entering Discovery from Catalog resets the sub-state to `Idle`.

## Verification

- Ran `cargo build`
- Result: success

## Acceptance Check

- `tasks/phase-3/task-3002.md` requirements implemented in the specified files
- `Screen::Discovery` dispatch added
- Discovery input handling added in `src/app.rs`
- `cargo build` succeeded
